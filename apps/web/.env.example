# demo o real. Nunca se cambia automáticamente cuando la API falla.
NEXT_PUBLIC_DATA_MODE=demo
# Solo servidor; incluir /api. Tu socio proporciona la URL publicada.
BACKEND_API_URL=http://localhost:3001/api
